var buttonClass = function(ismanage){
  return ismanage ? "active" : "";
}
var buttonText = function (ismanage) {
  return ismanage ? "宿舍长" : "设为宿舍长";
}

module.exports = {
  buttonClass: buttonClass,
  buttonText: buttonText
}